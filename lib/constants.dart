import 'package:flutter/material.dart';

/* Colours */
const buttonColour = Color(0xFF21ABA5);
const bckgroundColour = Color.fromARGB(255, 90, 227, 197);
final textColour = Color(0xFF10375C).withOpacity(0.8);

/* Font size */
const bigText = 60.0;
const mediumText = 25.0;
const smallText = 18.0;
